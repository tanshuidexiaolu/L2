#亲自出码1代码文件
import pygame
#pygame初始化
pygame.init()
canvas = pygame.display.set_mode((770, 660))
pygame.display.set_caption('缩放图片')

while True:
    #请在下方书写你的代码


    for event in pygame.event.get():
        if event.type == pygame.QUIT:
            exit()
